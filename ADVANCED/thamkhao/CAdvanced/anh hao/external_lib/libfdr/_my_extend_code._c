// Function: dll_find
// Check if a key is exist in dl list
// input: dl list, key
// output: 1 or 0 (exist or not)
int dll_find(Dllist l, int key)
{
	Dllist temp;
	dll_traverse(temp, l)
	{
		if (key == jval_i(temp->val))
		{
			return 1;
		}
	}
	return 0;
}