//#include "linked_list_arr.h"
#include "linked_list_single.h"
int main(){
	/* for array 
	arrLIST L;
	L.last = 2;
	L.elements[0] = 1;
	L.elements[1] = 2;
	L.elements[2] = 3;
	insert(4,2,&L);
	delete(2,&L);
	printLIST(L);
	*/

	/* linked_list_single
	node *root = makeNewNode(9,0);
	insert(&root,10,1);
	//printList(root);
	insert(&root,8,-1);	
	insert(&root,9,5);
	insert(&root,8,3);
	insert(&root,9,3);
	printList(root);
	delete(&root,3);
	printList(root);
	freeALL(&root);
	printList(root);
	*/
}