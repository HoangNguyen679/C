#include<stdio.h>
#include<BST.h>

typedef struct {
  int inf;} KeyType;
typedef struct Node{
  KeyType key;
  struc node* right,left;} NodeType;
typedef Node* TreeType;

void main() {
  TreeType *Root=NULL;
  int arr[100];
  printf("Nhap vao mot day so nguyen: ");
  int i;
  for(i=0;a[i]!=NULL;i++) {
    scanf("%d",&i);
    InsertNode(x,Root);}
  int b;
  printf("Enter the number u wanna find: ");
  scanf("%d",&b);
  Search(b,Root);
  return;}
